# Serialization & Reconstruction API

The `heraldo.serialization` module handles saving and loading optimization result dictionaries to disk, converting live circuit, target, and loss function instances into JSON-serializable configuration metadata, and reconstructing object instances upon loading.

```{eval-rst}
.. automodule:: heraldo.serialization
   :members:
   :undoc-members:
   :show-inheritance:
```